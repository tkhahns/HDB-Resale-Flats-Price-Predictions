{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm ci",
  "framework": "nextjs",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- . ':(exclude)../data/**' ':(exclude)../reports/**'"
}
